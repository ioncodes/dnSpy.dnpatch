<UserControl x:Class="dnSpy.dnpatch.ToolWindowControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:dnSpy.dnpatch"
             xmlns:mvvm="clr-namespace:dnSpy.Contracts.MVVM;assembly=dnSpy.Contracts.DnSpy"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <!-- InitDataTemplateAP.Initialize will initialize the context menu to prevent the default
         tool window context menu from showing. -->
    <Grid
        mvvm:InitDataTemplateAP.Initialize="True"
        Background="Transparent" Margin="0,0,-522,0"
        >
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <ListView x:Name="listView" Grid.Column="1" HorizontalAlignment="Left" Height="275" Margin="10,12,0,-282" Grid.Row="3" VerticalAlignment="Top" Width="802">
            <ListView.View>
                <GridView>
                    <GridViewColumn DisplayMemberBinding="{Binding Path}"  Header="Path" Width="200"/>
                    <GridViewColumn DisplayMemberBinding="{Binding Method}"  Header="Method" Width="100"/>
                    <GridViewColumn DisplayMemberBinding="{Binding Instructions}"  Header="Instructions" Width="150"/>
                    <GridViewColumn DisplayMemberBinding="{Binding Indices}"  Header="Indices" Width="150"/>
                </GridView>
            </ListView.View>
        </ListView>
    </Grid>
</UserControl>